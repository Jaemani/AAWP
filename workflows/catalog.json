{
  "schemaVersion": "aawp/studio-workflow-catalog/v1",
  "workflows": [
    {
      "workflowPath": "workflows/templates/spec-to-demo/workflow.wir.yaml",
      "executionManifestPath": "workflows/templates/spec-to-demo/execution.manifest.json",
      "displayName": "Spec to demo",
      "description": "Source spec에서 명시적으로 선택한 화면만 동작 가능한 웹 데모로 만들고 독립 검증합니다.",
      "inputKind": "spec-to-demo"
    },
    {
      "workflowPath": "examples/spec-feedback-to-spec.wir.yaml",
      "executionManifestPath": "workflows/templates/spec-feedback-to-spec/execution.manifest.json",
      "displayName": "Spec feedback to spec",
      "description": "기준 Spec과 stable-ID 피드백을 고정하고 검토 가능한 immutable child Spec candidate와 성숙도 보고서를 생성합니다.",
      "inputKind": "spec-feedback-to-spec"
    }
  ]
}
